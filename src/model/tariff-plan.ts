export class TariffPlan {
  id: number;
  code: string;
  // tslint:disable-next-line:variable-name
  camera_count: number;
  price: number;
  // tslint:disable-next-line:variable-name
  record_day_count: number;
  // tslint:disable-next-line:variable-name
  access_to_panel: boolean;
}
